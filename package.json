{
  "name": "accurapp",
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap",
    "publish": "tasks/release.sh",
    "outdated": "for P in $(ls packages); do echo $P; cd \"packages/$P/\"; yarn outdated; cd ../..; done"
  },
  "devDependencies": {
    "lerna": "^2.0.0-rc.3"
  }
}
