{
  "name": "accurapp",
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap",
    "cleaninstall": "lerna clean",
    "lint": "node ./node_modules/eslint/bin/eslint.js packages",
    "publish": "tasks/release.sh",
    "outdated": "for P in $(ls packages); do echo '\\n' === $P ===; cd \"packages/$P/\"; yarn outdated; cd ../..; done",
    "create-test-app": "rm -rf test-app $(yarn cache dir)/*accurapp*; node packages/create-accurapp/index.js test-app --testing"
  },
  "devDependencies": {
    "lerna": "^2.0.0-rc.3",
    "eslint-config-accurapp": "file:./packages/eslint-config-accurapp"
  }
}
